#### 1. 在本地终端新建并切换分支  
切换分支使用 `git checkout branch-name`  
如果是新建并切换则 `git checkout -b brach-name`  
使用 `git branch -a` 可查看分支列表以及当前在哪个分支

#### 2. 在新分支进行开发测试

#### 3. 完成后在github通过pull request合并到main分支

#### 4. 等待网站构建完成，确定没问题后删除开发用的分支
记得本地切换回main并使用 `git branch -d branch-name` 删除本地开发分支